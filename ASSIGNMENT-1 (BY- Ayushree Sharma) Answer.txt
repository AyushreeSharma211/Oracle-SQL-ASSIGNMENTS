ASSIGNMENT-1 (BY- Ayushree Sharma)

1.select count(*) from emp;
2.select count(*) from dept;
3.select ename from emp;
4.select deptno,dname from dept;
5.select sum(sal) from emp;
6.select sum(comm) from emp;
7.select distinct(job) from emp where comm>0;
8.select distinct(to_char(sysdate,'DD-MM-YYYY'))"Now" from emp;
9.select round(avg(sal),2) from emp;
10.select deptno,count(empno) from emp group by deptno order by 1;
11.select deptno, sum(sal) from emp group by deptno order by 1;
12.select job,count(empno) from emp group by job order by 1;
13.select job, round(avg(sal),2) from emp group by job order by 1;
14.select empno, ename,to_char(hiredate,'DAY') as Day, to_char(hiredate,'MONTH') as Month, to_char(hiredate,'YYYY') as Year from emp;
15.select empno,deptno,ename from emp order by deptno;
16.select empno,job,ename from emp order by job;
17.select empno,ename,job,sal from emp order by sal desc;
18.select deptno,empno, ename,sal from emp order by deptno, sal desc;
19.select count(ename) from emp where length(ename)=6;
20.select max(sal), min(sal) from emp;
21.select deptno, max(sal), min(sal), round(avg(sal),2), sum(sal) from emp group by deptno order by 1;
22.select empno, ename, hiredate from emp order by hiredate;
23.select empno, ename, hiredate from emp where hiredate=(select max(hiredate) from emp);
24.select empno, ename, hiredate from emp where hiredate=(select min(hiredate) from emp);
25.select deptno, empno, ename, hiredate from emp order by hiredate desc, deptno;
26.select empno, ename,sal from emp where sal>=(select avg(sal) from emp);
27.select empno, ename,sal from emp where sal<=(select avg(sal) from emp);
28.select empno, ename, sal from emp where sal between 2000and 4000;
29.select empno, ename, sal from emp where sal =(select max(sal)from emp) or sal=(select min(sal) from emp);
30.select count(empno) from emp where to_char(hiredate,'MM')=1;